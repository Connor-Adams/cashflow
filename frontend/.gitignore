# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Bookmarklet IIFE bundles are built into public/ so vite can serve them from
# the same origin (no static-host hop needed). They're generated artifacts —
# never commit them.
public/bookmarklets

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
