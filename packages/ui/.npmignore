# Belt-and-suspenders guard alongside the "files" allowlist in package.json.
# dist/ and src/styles/ ship intentionally — do NOT exclude them.

# Source (components, lib, tests — only src/styles ships via "files" allowlist)
src/components
src/lib
src/__smoke__
src/index.ts

# Scripts
scripts

# Build configs
*.config.ts
vitest.setup.ts
tsconfig.json
tsconfig.*.json

# Dev artifacts
coverage
.nyc_output

# Editor / OS
.DS_Store
*.local
